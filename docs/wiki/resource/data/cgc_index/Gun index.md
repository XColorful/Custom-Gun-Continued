[English](#English)

# 枪械索引
> wiki 版本：`0.0.5`.1

- `name_lang`/`name`（String）：
- `tooltip_lang`/`tooltip`（String）：
- `data_location`/`data`（ResourceLocation）：
- `display_index_location`/`display`（ResourceLocation）：
- `slot_sort`/`sort`（int）：
---
- `gun_category`/`type`（GunCategory）：
- `item_type`（ItemType）：

```json
{
	"name_lang": "",
	"tooltip_lang": "",
	"data_location": "namespace:location",
	"display_index_location": "namespace:location",
	"slot_sort": 0,
	
	"gun_category": "",
	"item_type": ""
}
```

# English
> wiki verison: `0.0.5`.1

- `name_lang`/`name`(String):
- `tooltip_lang`/`tooltip`(String):
- `data_location`/`data`(ResourceLocation):
- `display_index_location`/`display`(ResourceLocation):
- `slot_sort`/`sort`(int):
---
- `gun_category`/`type`(GunCategory):
- `item_type`(ItemType):

```json
{
	"name_lang": "",
	"tooltip_lang": "",
	"data_location": "namespace:location",
	"display_index_location": "namespace:location",
	"slot_sort": 0,
	
	"gun_category": "",
	"item_type": ""
}
```