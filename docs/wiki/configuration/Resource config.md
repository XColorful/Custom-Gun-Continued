[English](#English)

# 资源配置
> wiki 版本：`0.0.1`.0

路径：`resource`
- `EnableLazyClientAssetLoad`（bool）：是否启用客户端资产懒加载
```toml
[resource]
	EnableLazyClientAssetLoad = true
```

# English
> wiki verison: `0.0.1`.0

## Resource Config

Path: `resource`
- `EnableLazyClientAssetLoad` (bool): Whether to enable client asset lazy loading
```toml
[resource]
	EnableLazyClientAssetLoad = true
```
