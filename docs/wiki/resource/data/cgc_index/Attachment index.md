[English](#English)

# 配件索引
> wiki 版本：`0.0.5`.1

- `name_lang`/`name`（String）：
- `tooltip_lang`/`tooltip`（String）：
- `data_location`/`data`（ResourceLocation）：
- `display_index_location`/`display`（ResourceLocation）：
- `slot_sort`/`sort`（int）：
---
- `attachment_category`/`type`（AttachmentCategory）：
- `hide_in_game`/`hidden`（boolean）：

```json
{
	"name_lang": "",
	"tooltip_lang": "",
	"data_location": "namespace:location",
	"display_index_location": "namespace:location",
	"slot_sort": 0,
	
	"attachment_category": "",
	"hide_in_game": false
}
```

# English
> wiki verison: `0.0.5`.1

- `name_lang`/`name`(String):
- `tooltip_lang`/`tooltip`(String):
- `data_location`/`data`(ResourceLocation):
- `display_index_location`/`display`(ResourceLocation):
- `slot_sort`/`sort`(int):
---
- `attachment_category`/`type`(AttachmentCategory):
- `hide_in_game`/`hidden`(boolean):

```json
{
	"name_lang": "",
	"tooltip_lang": "",
	"data_location": "namespace:location",
	"display_index_location": "namespace:location",
	"slot_sort": 0,
	
	"attachment_category": "",
	"hide_in_game": false
}
```