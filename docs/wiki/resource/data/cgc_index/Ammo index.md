[English](#English)

# 子弹索引
> wiki 版本：`0.0.5`.1

- `name_lang`/`name`（String）：
- `tooltip_lang`/`tooltip`（String）：
- `display_index_location`/`display`（ResourceLocation）：
- `slot_sort`/`sort`（int）：
---
- `max_stack_size`/`stack_size`（int）：

```json
{
	"name_lang": "",
	"tooltip_lang": "",
	"display_index_location": "namespace:location",
	"slot_sort": 0,
	
	"max_stack_size": 0
}
```

# English
> wiki verison: `0.0.5`.1

- `name_lang`/`name`(String):
- `tooltip_lang`/`tooltip`(String):
- `display_index_location`/`display`(ResourceLocation):
- `slot_sort`/`sort`(int):
---
- `max_stack_size`/`stack_size`(int):

```json
{
	"name_lang": "",
	"tooltip_lang": "",
	"display_index_location": "namespace:location",
	"slot_sort": 0,
	
	"max_stack_size": 0
}
```