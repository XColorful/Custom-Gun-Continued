# 

```
现在需要解决26.2的一个渲染问题：
- 该问题在26.2.0.6-beta和26.2.0.88客户端/模组版本均有（排除最近commit更新neoforge环境导致的差异）
- 手持Beryl M762切换到装配界面，点击瞄准镜，卸下4倍镜（此时4倍镜的ocular在屏幕约中心的位置），然后切换到未选择配件状态下的装配界面，在卸下4倍镜时的ocular位置，枪械模型是透明（能看见后面的风景）的，并且退出再打开装配界面仍然存在，切到其他物品再切回来时才清除
- 当前为server，我复制了一份包在专用服务端目录下.\tacz\classic_battleroyale_gun\data\cbrg\data\guns\beryl_m762_data.json
- 客户端有装CGC-Compat-PackLoader，所以可以用tacz目录读取数据包和资源包，不需要考虑是读取文件错误的问题

- 当前IDEA打开了26.2，26.1.2，1.20.1供浏览源码，TaCZ只有1.20.1且CGC1.20.1-26.1.2都正常，1.20.1差异无参考价值
- CGC渲染体系文档在.\docs\architecture\client\render\下
- 当前正在完成26.3分支移植，但是需要先修复26.2引入的问题，从而避免26.3测试移植时被老版本bug误判，并且修复时不要以26.3的源码作为参考
```

# 

我先读取相关记忆和渲染文档，理解已有的排查结论。

记忆 5 条直接相关（stencil 清空）。先读渲染文档与相关源码。

Let me look at the refit screen's unload flow and the transform state.

Let me check the vanilla 26.2 stencil lifecycle via IDEA MCP.

Found a strong candidate root cause. Let me verify the stencil state accumulation logic.

Let me verify the submit/flush ordering in vanilla 26.2 to confirm the fix direction.

Root cause found and verified against 26.1.2. Applying the fix.

Now let me verify it compiles.

Compiles clean. Let me record the non-obvious platform pitfall in the existing 26.2 memory.

## 根因

不是资源读取问题，是**模板（stencil）缓冲区残留 + 继承下来的模板测试**，而且正好是 26.2 移植时漏掉的一行。

调用链（以你的复现步骤为准）：

1. 4 倍镜（长筒镜）装在 SCOPE 槽时，[_GunModelRender.java:159-167](core/src/main/java/dev/xcolorful/customgun/client/model/_GunModelRender.java:159) 给枪体开出模板测试 `GL_EQUAL 0`（镜内不画枪体），并把这个状态累积在 `ClientRenderHelper.GL.stencilState` 里；目镜在模板缓冲写下圆形值（`_AttachmentModelRender.renderOcularAndDivision` 的 `INVERT` 三角扇）。
2. 卸下 4 倍镜后，`_renderScope` 在 [第 139 行](core/src/main/java/dev/xcolorful/customgun/client/model/_GunModelRender.java:139) 提前返回，**不再有清空**，也不再设模板测试。但 [第 121 行](core/src/main/java/dev/xcolorful/customgun/client/model/_GunModelRender.java:121) 的 `_stencilOp(KEEP,KEEP,KEEP)` 无条件调用 `applyStencil(stencilState)`，把上一帧累积的 `EQUAL 0`（ref=0）重新写回 `CURRENT_STENCIL` → 枪体在 submit 阶段又按这个测试烘焙了管线。
3. 于是枪体"只画 stencil == 0 的地方"，而缓冲区里还留着 4 倍镜目镜那个圆 → 圆形区域内枪体被裁掉，露出后面的世界。残留没人清，所以关掉再打开装配界面依然在。

关键证据：`git diff 26.1.2neoforge 26.2neoforge` 显示 `ClientRenderHelper._stencilOp` 和 `NeoStencilOperator` **完全一致**，唯一的差异是 [`_GunModelRender._clearStencilBuffer()`](core/src/main/java/dev/xcolorful/customgun/client/model/_GunModelRender.java:247) 的实现在 26.2 被注释掉了（`[26.2, )` 分支留空）。26.1.2 每帧枪械渲染结束时都会清空整个模板缓冲区，所以那个继承下来的测试读到的是全 0、恒通过，问题被掩盖。1.21.6–26.2 的 `clearColorAndDepthTextures` 只清颜色+深度，不清 stencil，残留才会活下来。

## 改动

只改了一处：[`_GunModelRender._clearStencilBuffer()`](core/src/main/java/dev/xcolorful/customgun/client/model/_GunModelRender.java:266) 的 `[26.2, )` 分支补上 `clearStencilTexture`，与同文件里 `_AttachmentModelRender._clearStencilBuffer` 以及 26.1.2 的写法一致。立即执行、submit 阶段清空 → flush 阶段写入，顺序仍是「清空→写入」，所以开镜镜内效果不受影响。IDEA 编译已通过（只有既有的无关 deprecation warning）。

## 需要你验证

我无法在这里跑客户端看画面，请你按原步骤复现一遍：装配界面 → 瞄准镜 → 卸下 4 倍镜 → 切到未选择配件状态。预期圆形区域不再透明；顺带留意开镜时镜内仍然不画枪体（这是同一个测试在起作用的路径）。

## 一个遗留问题（要不要一并修，你定）

真正的源头是 `GL._stencilOp()/_stencilFunc()/_stencilMask()` **无条件**调 `applyStencil`，等于在模板测试已 disable 后仍能把它"重新启用"——26.2 下 `CURRENT_STENCIL` 是否存在就是启用开关，语义上应该只在 `enableItemEntityStencilTest()` 之后才写回。这个行为 26.1.2 完全相同（被每帧清空掩盖），所以不是 26.2 引入的，但它是颗雷：一旦某版本把清空时机挪到绘制之后，就会以同样方式炸。修法是给 `ClientRenderHelper.GL` 加一个 `enabled` 标志，`_stencilXXX` 只在启用时才 `applyStencil`，保持 GL 语义。这动了模板兼容层的语义（属于架构意图），所以我先只做了恢复 26.1.2 行为的最小修复，没有擅自改它——你要的话我再动。

改动未提交。

后台那条搜索命令（早前超时转后台的 `batchKey` 全仓库 grep）已结束，结果无用——同样的信息我已经用定向检索拿到了：`CustomFeatureRendererSubmitMixin` 在 core 里存在且已登记进 `customgunclient.mixins.json`，提交顺序保序是生效的。

无新增动作。之前的改动（`_GunModelRender._clearStencilBuffer` 补上 26.2 清空）仍未提交，等你游戏内验证；遗留的 `_stencilOp` 无条件写回问题也仍等你定夺是否一并修。

# 

```
@"D:\Github\Custom-Gun-Continued-1.21.1neoforge/" @"D:\Github\Custom-Gun-Continued-1.21.4neoforge/" @"D:\Github\Custom-Gun-Continued-1.21.6neoforge/" @"D:\Github\Custom-Gun-Continued-1.21.11neoforge/" 修复了。

我现在想了几个方案：
1. 补一个[26.2, )注释说明，把26.2的代码注释掉放进1.20.1-26.1.2，从而弥补26.2不会自动刷新模板
2. 照你说的往GL里加bool，但是需要确定在1.20.1-26.2里是统一的，在非延迟渲染下也要正常工作
3. 把因为没有提前返回而造成的问题给修了，从而避免继续执行后面的模板

方案1也就是让1.20.1-26.1.2创造一片注释掉的区域，完全无影响
方案2需要测试每个版本的情况
方案3提前返回是否会影响其他地方逻辑？

根据你研究到的，分析下哪个合适？
当前打开了1.21.1，1.21.4，1.21.6，1.21.11，26.1.2的窗口供浏览渲染
需要尽可能避免每个版本都适配一次
```

# 

修复确认。三个方案我需要先核对各版本的实际状态，再给结论。

已核对完 1.20.1 / 1.21.1 / 1.21.4 / 1.21.6 / 1.21.11 / 26.1.2 六个分支的同一批代码，结论有依据了。

## 关键证据

| 版本 | `GL._stencilFunc/Op/Mask` 走哪条路 | `_GunModelRender._clearStencilBuffer` | 会不会复现 |
|---|---|---|---|
| 1.20.1 / 1.21.1 / 1.21.4 | 立即模式 `RenderSystem.stencilXXX`，由 `glEnable/glDisable(GL_STENCIL_TEST)` 门控 | 有（`GL._clear`） | 不会→缺陷根本不存在 |
| 1.21.6 / 1.21.11 / 26.1.2 | 累积 `stencilState` + 无条件 `applyStencil` | **有**（`clearStencilTexture`） | 缺陷存在，但每帧被清空掩盖 |
| 26.2 | 同上，与 26.1.2 逐字相同 | **空**（移植时注释掉，`[26.2, )` 留白） | 暴露 |

两点因此确定：

1. **「累积状态被重新写回 `CURRENT_STENCIL`」不是 26.2 引入的**，1.21.6–26.1.2 一模一样，只是那三版每次枪械渲染都清一遍模板缓冲，于是被继承下来的 `EQUAL 0` 读到的全是 0、恒通过。
2. **26.2 真正缺的就是那一行清空**，其余版本的 `[1.21.6, 26.2)` 区域都是活的。这是移植漏项，不是设计变更。

## 三个方案的评估

**方案 1**（补上清空 + 注释；已实现）
- 只动 26.2 那一个 `[26.2, )` 区域，与 26.1.2 行为逐字节对齐。**零跨版本影响，零跨版本测试**。
- 代价：26.2 每次枪械渲染多一次全屏 stencil clear（1.21.6–26.1.2 本来就有），且根部缺陷仍在。
- 符合仓库既有约定（版本区间注释 + 只启用当前区间），符合「补漏项」而非「改架构」。

**方案 2**（`GL` 里加 bool）
- 需要澄清一点：它**不需要「每个版本都适配一次」**。把 flag 的写入放在 `enableItemEntityStencilTest`/`disableItemEntityStencilTest` 的版本中性位置，把 `applyStencil` 的条件读放在 `[1.21.6, )` 区间内 —— 而 1.20.1–1.21.4 的这个区间被 `if (true) return;` 挡住，是死代码。所以是**单文件、约 5 行、对 1.20.1–1.21.4 完全惰性**。
- 代价只剩「验证」：1.21.6 / 1.21.11 / 26.1.2 / 26.2 各测一次开镜镜内、组合镜、红点、机瞄。行为确实变了（`_stencilOp` 在没瞄具的帧不再写回），所以不能只靠推理。
- 收益：这是唯一能消掉这一类缺陷的改法，而且做掉之后 **26.2 那行清空就可以不要了**——26.3 那边模板载体要挪进 `DepthStencilState`，这块本来就要重做，留个语义正确的基座更划算。

**方案 3**（提前返回，跳过后续模板代码）
- 对 1.20.1–1.21.4：跳过那条 `RenderSystem.stencilOp` 是无害的（测试本来就关着），但要动 `_renderScope` 的签名让它回传「有没有开模板测试」，等于**在 26.3 即将重写的同一个函数上先动一刀**。
- 它只堵住当前唯一的活动调用点（`_GunModelRender:121`），没改语义：以后任何在 enable 窗口之外调 `GL._stencilXXX` 的地方都会重现同类问题。
- 跨版本爆炸半径与方案 2 相同（都要重测 4 个分支），但收益更小。**三者里最不划算。**

## 建议

**现在按方案 1 收口**，理由：它是纯粹的移植漏项恢复，和 26.1.2 行为一致，不需要任何其它版本的回归测试——正合你「避免每个版本适配一次」的约束；当前改动已编译通过，等你游戏内确认即可。

**方案 2 作为独立的一次改动排在 26.3 之前**（或与 26.3 模板载体迁移一起做）。它不是「每版本适配」，只是「每版本验证 4 次开镜」，而这份验证你在推 26.3 时本来就要做一遍；做完后 26.2/26.3 都不再需要那行全屏清理。要不要我现在就把方案 2 的补丁写出来给你一起验，你定；我这边没有跑过任何版本的客户端，两条路的运行结果都得由你实测确认。

（另：方案 1 的注释已写进 `[26.2, )` 区域；如果你想让它同时充当「给 1.20.1–26.1.2 的读者看的前向占位」，我可以把同一段注释短语也补到其它分支的 `[26.2, )` 空白区——纯注释、零影响。）

# 

```
- 先在当前打开的1.20.1,1.21.1neoforge,1.21.4neoforge,1.21.6neoforge,1.21.11neoforge,26.1.2neoforge都实现方案2
- 如果以上各版本之间的实现有差异，需要像已有的“[1.20.1, 1.21.4)然后不同版本取消不同注释行来生效”这种风格
- 不需要cherry pick，直接临时修改上去，我测试后再决定要不要从1.20.1 commit然后merge到高版本
- 26.2暂时不动，防止忘掉方案1的修复
```

# 

明白。先摸清各分支该文件的差异，再决定注释区间怎么切。

各分支该文件的差异只在不相关的部分——模板 helper 三个高版本（1.21.6/1.21.11/26.1.2）实现形状完全一致，低版本那段是 `if (true) return;` 死代码。所以补丁可以逐字一致，不需要按版本分叉。先读各文件确认锚点。

锚点全部确认，六个文件在这两处的文本一致。开始逐个打补丁。

六个文件都改完了。现在核对每处改动与残留的 `applyStencil` 调用点。

改动形状正确。现在逐个项目编译验证（含低版本那段死代码是否仍能编译）。

六个项目全部编译通过。最后核对一次调用点是否都在启用窗口内。

方案 2 已落到六个版本（工作区改动，未提交），每个文件都是 22 insertions / 3 deletions 的同一形状，26.2 未动。

## 结论：不需要按版本分叉

我把六个分支该文件比对了一遍，模板 helper 只有两种形态，而补丁正好落在已有的版本注释区间里：

- **1.21.6 / 1.21.11 / 26.1.2**：`[1.21.6, )` 区间是活的，`_stencilFunc/_stencilOp/_stencilMask` 以累积 `stencilState` + 无条件 `applyStencil` 结束 → 三处加 `if (stencilEnabled)`。
- **1.20.1 / 1.21.1 / 1.21.4**：同一段代码在 `if (true) return; // 让IDE保留下面的引用关系` 之后，是死代码 → 同样的三处 `if (stencilEnabled)` 放进去零影响。

所以六个文件用的是**逐字相同**的补丁：`GL` 里加 `stencilEnabled` 字段、`enableItemEntityStencilTest`/`disableItemEntityStencilTest` 各加一行置位、上面三处包一层。既然实现无差异，就没有动用 `[x, y)` 分叉注释；这也意味着你之后从 1.20.1 做正式 commit 再 merge 上去时不会撞冲突。

## 关键收益：三个低版本不用测

1.20.1 / 1.21.1 / 1.21.4 的启用/停用走 `glEnable/glDisable(GL_STENCIL_TEST)`，setter 走 `RenderSystem.stencilXXX`，**本来就没有这个缺陷**。加上去的那行只是写一个「只有死代码会读」的字段，行为逐字节不变。**只有 1.21.6 / 1.21.11 / 26.1.2（以及将来并入方案 2 的 26.2）需要实测。**

## 这个改动为什么是保守的

`stencilState` 的字段赋值全部保留，被门控的只是「写回管线/RenderSystem」这一个动作，而 `enableItemEntityStencilTest()` 会把记录下来的完整状态重新生效——所以**不可能丢失任何模板设置**，只消除了「停用之后又被 setter 重新启用」这一种效果。

我核对了该分支上全部 24 个 `_stencilFunc/_stencilOp/_stencilMask` 调用点：**只有 `_GunModelRender:121` 的 `_stencilOp(KEEP,KEEP,KEEP)` 落在启用窗口之外**（无瞄具帧），其余 23 个都在 `enable…/disable…` 之间。所以**带瞄具的渲染路径逐字不变**，唯一的行为变化就是无瞄具帧里枪体不再被上一次瞄具留下的测试裁剪。

## 已做与待做

六个项目都用 IDEA 编译通过（只有既有的 deprecation warning，无 error）。各工作区现状：

| 工作区 | 改动 |
|---|---|
| `Custom-Gun-Continued`（1.20.1） | `ClientRenderHelper` 方案 2 |
| `...-1.21.1neoforge` / `-1.21.4neoforge` / `-1.21.6neoforge` / `-1.21.11neoforge` / `-26.1.2neoforge` | 同上 |
| `...-26.2neoforge` | 仍只有方案 1 的 `_GunModelRender` 清模板 |

待你实测的三个版本，建议每次按同一顺序过一遍：装 4 倍镜开镜看镜内画面与遮罩 → 关镜 → 卸下 4 倍镜 → 切到未选择配件状态看还有没有圆形透明 → 再看红点/机瞄是否正常（若有组合镜一并看）。

我没有跑过客户端，编译结果不能替代画面确认。另外提醒一句：方案 2 验证通过后，26.2 那行「每帧清空模板」就变成多余（清空本身无害，只是每次枪械渲染多一条 GPU 清屏指令），保留还是撤掉由你定；在 26.3 落地前我建议先留着。

# 

```
都没问题，26.2改用方案2的补丁我看看
```

# 

收到。给 26.2 换成方案 2，并把方案 1 的兜底清空撤掉——这样 26.2 的改动形状就和其他六个版本一致，你看到的就是「方案 2 单独是否成立」。

26.2 已换成方案 2，方案 1 完全撤掉。

**改动**：`ClientRenderHelper` 加了 `stencilEnabled` 开关（22 insertions / 3 deletions，与其余六个版本**逐字相同**）；`_GunModelRender._clearStencilBuffer` 已回到 HEAD——`git diff --exit-code` 无差异，没留任何残留注释或死代码。`git status` 现在只有 `ClientRenderHelper.java` 一个文件，diff 面板已打开。

26.2 也编译通过（这次连 warning 都没有）。

## 测试时值得留意的一点

撤掉清空后，26.2 成了唯一「不每帧清模板」的分支：目镜写下的圆形值会一直留在模板缓冲区里。这在推理上是无害的——没有瞄具时模板窗口不会打开，`bakePipelineState` 不会给任何几何体注入测试，那份残留没有任何读取者；而一旦装上瞄具，`_AttachmentModelRender._clearStencilBuffer()`（这条仍在）会在写入前清空整片缓冲区。但这正是本次改动**唯一新引入的状态假设**，所以测试时请留意两类情况：

- 原复现步骤：装 4 倍镜 → 开镜 → 卸下 → 切到未选择配件状态，看圆形透明是否消失。
- **连续换枪**：从装了瞄具的枪切到没装瞄具的枪（尤其两把枪瞄具位置不同时），看枪体有没有出现不该有的圆形裁剪——这是残留值唯一可能被误读的路径。

其余三个高版本（1.21.6 / 1.21.11 / 26.1.2）仍各自保留着那行清空，所以你那边「都没问题」的结论只覆盖方案 2 本身；如果这次 26.2 也过了，可以考虑之后把这三个分支的清空一并撤掉做全版本对齐（属于清理，不是修 bug，你定）。

方案 1 的那段兜底（`[26.2, )` 里的 `clearStencilTexture`）我没有以注释形式保留——怕将来读代码的人分不清哪条是生效路径。相关机制和「1.21.6–26.1.2 是靠清空掩盖、并非没有缺陷」这一点我记进项目记忆了，26.3 移植时不会丢。要恢复兜底随时说一句。

改动仍未提交。

# 

```
26.2没问题，我已经手动把1.21.1-26.2的临时补丁都撤了，你现在修改一下临时写在1.20.1的注释，需要额外补充这个修改能弥补26.2的差异（说明26.2不会自动清理+能工作），注释最好用<ul><li>来写，每一断句/点用一个<li>
```

# 

明白，改成 `1.20.1` 单一来源的注释，并补上 26.2 差异的说明。先确认各工作区现状。

各工作区确认：只有 1.20.1 带补丁，1.21.1–26.2 都已干净回到 HEAD。现在改注释。

注释已按 `<ul><li>` 重写，分三组（1.21.6 起 / 26.2 / [1.20.1, 1.21.6)），每个断句一个 `<li>`，26.2 组明确写了「不会自动清理模板缓冲区」和「本改动使其不需要那一步清空即可正常工作」。

```
166         /**
167          * 模板测试当前是否启用。
168          * <ul>
169          *     1.21.6 起
170          *     <li>「启用/停用模板测试」被表示为「有没有把模板测试写回管线」</li>
171          *     <li>{@link #_stencilFunc}/{@link #_stencilOp}/{@link #_stencilMask} 会把累积状态无条件写回</li>
172          *     <li>于是停用之后随便一次 setter 都能把测试重新启用，读到的还是上一帧瞄具留下的状态</li>
173          *     <li>表现为卸下瞄具后枪体在残留的圆形区域被裁掉</li>
174          *     <li>这里按 GL 语义补开关：只在启用期间才写回，未启用时仅记录状态、等 enable 时统一生效</li>
175          * </ul>
176          * <ul>
177          *     26.2
178          *     <li>26.2 的延迟渲染框架不会自动清理模板缓冲区（GameRenderer 开头只清颜色与深度），目镜写下的模板值会一直残留</li>
179          *     <li>1.21.6-26.1.2 靠「每次枪械渲染结束时清空整个模板缓冲区」掩盖了同一问题，26.2 移植时漏掉了这一步</li>
180          *     <li>本改动让没有瞄具的帧不再继承上一次瞄具的模板测试，残留值失去读取者</li>
181          *     <li>因此 26.2 不需要那一步清空也能正常工作，该清空在 1.21.6-26.1.2 上也随之变为冗余</li>
182          * </ul>
183          * <ul>
184          *     [1.20.1, 1.21.6)
185          *     <li>即时模式的模板状态由 glEnable/glDisable 门控，没有这个问题</li>
186          *     <li>三个 setter 的 [1.21.6, ) 分支在此区间是死代码，所以本字段在 1.20.1-1.21.4 上不带来任何行为差异</li>
187          * </ul>
188          */
189         @ApiStatus.AvailableSince("1.21.6")
190         private static boolean stencilEnabled = false;
```

[1.20.1 的 ClientRenderHelper.java](D:/Github/Custom-Gun-Continued/core/src/main/java/dev/xcolorful/customgun/client/util/ClientRenderHelper.java:166)

编译通过（只有既有 deprecation warning）。

两点说明：

- 我把 26.2 组放中间而不是最后，是因为它是这条注释存在的理由；如果你希望顺序按版本递增（1.20.1 → 1.21.6 → 26.2），说一句我调整。
- 第 181 行提到的「该清空在 1.21.6-26.1.2 上也随之变为冗余」是给将来做全版本对齐的人留的线索；如果你不希望注释里出现「将来可以清理」这类信息，我删掉这一句。

现在只有 1.20.1 工作区有改动（`ClientRenderHelper`），1.21.1–26.2 都是干净的 HEAD，等你从 1.20.1 commit 后 merge 上去。未提交。
