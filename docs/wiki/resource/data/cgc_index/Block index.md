[English](#English)

# 方块索引
> wiki 版本：`0.0.0`.0

- `name_lang`/`name`（MutableComponent）：
- `tooltip_lang`/`tooltip`（MutableComponent）：
- `data_location`/`data`（ResourceLocation）：
- `display_index_location`/`display`（ResourceLocation）：
- `slot_sort`/`sort`（int）：
---
- `block_location`/`id`（ResourceLocation）：
- `max_stack_size`/`stack_size`（int）：

```json
{
	"name_lang": "",
	"tooltip_lang": "",
	"data_location": "namespace:location",
	"display_index_location": "namespace:location",
	"slot_sort": 0,
	
	"block_location": "namespace:location",
	"max_stack_size": 0
}
```

# English
> wiki verison: `0.0.0`.0

- `name_lang`/`name`(MutableComponent):
- `tooltip_lang`/`tooltip`(MutableComponent):
- `data_location`/`data`(ResourceLocation):
- `display_index_location`/`display`(ResourceLocation):
- `slot_sort`/`sort`(int):
---
- `block_location`/`id`(ResourceLocation):
- `max_stack_size`/`stack_size`(int):

```json
{
	"name_lang": "",
	"tooltip_lang": "",
	"data_location": "namespace:location",
	"display_index_location": "namespace:location",
	"slot_sort": 0,
	
	"block_location": "namespace:location",
	"max_stack_size": 0
}
```