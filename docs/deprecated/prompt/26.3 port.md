# 

```
模组新增26.3分支，反复编译修复直到编译通过
- 当前IDEA打开了26.2和1.20.1供浏览不同版本差异

目前第一个编译不通过的类：
D:\Github\Custom-Gun-Continued-26.3neoforge\core\src\main\java\dev\xcolorful\customgun\client\api\minecraft\access\IClientAccessTransformer.java:3: error: cannot find symbol
import com.mojang.blaze3d.pipeline.RenderPipeline;
                                  ^
  symbol:   class RenderPipeline
  location: package com.mojang.blaze3d.pipeline
```

# 

I'll start by understanding the current state of the 26.3 branch and the compilation error.

This is the key: 26.3 reorganizes `blaze3d` into a new `renderpearl` package. Let me map the new structure.

Now let me kick off the compile to get the ground truth error list while I explore.

Let me check the compile progress.

Build resolved the 26.3 dependency. Now let me locate the decompiled Minecraft sources to map the new class locations.

The decompiled sources are available locally. Let me map the full package reorganization.

Now I have the mapping. Let me read the new 26.3 API sources.

The decompiled 26.3 sources are also now in the project's build dir. Let me find the affected CGC sources.

Now let me dig into the 26.3 hand-render refactor.

Now let me check the mixin config and the hand-render event.

Now I'll make the mechanical package moves first.

Now let me read the call sites I need to adapt.

I have the full picture. Let me fix the pipeline modifiers first.

Now the stencil operator. Let me first check how NeoForge 26.3 itself carries stencil.

Now let me rework the NeoForge stencil operator for the merged depth-stencil state.

Now let me recompile.

More errors surfaced now that the earlier ones are fixed. Let me collect the full list.

This is a much larger port than the first 19 errors suggested. Let me survey the new 26.3 APIs for each error category.

This is a substantial multi-subsystem port. Let me set up tracking and gather the remaining API facts.

Now I have all the API facts. Let me start applying fixes, beginning with the bulk mechanical ones.

Let me recompile and see where we stand.

Good — core is nearly clear. Let me see the full remaining list and details.

Core compiles cleanly now. Let me handle the compat-module errors.

Now let me fix the access transformer and the remaining compat errors.

Now let me recompile.

Compilation passes for both modules. Let me verify a full build (resources, mixin AP, jar).

Build is green. Let me verify the mixin refmap actually picked up the new mixins.

Let me verify the `mulPose`→`rotate` semantic equivalence in the 26.3 source.

Build is green and the worktree is clean. Let me record the 26.2→26.3 API deltas in memory.

26.3 分支现在 `./gradlew build` 全绿（core + neoforge-compat，含资源处理与 jar）。

## 最初那个错误的原因

26.3 把渲染后端从 `blaze3d` 拆到了新的 `com.mojang.renderpearl` 包树，`RenderPipeline` / `ColorTargetState` / `DepthStencilState` / `PrimitiveTopology` 全部换了包。修掉之后暴露出 75 个错误，分属十来个子系统。

## 处理的主要变更

**包迁移（机械）**：`blaze3d.pipeline.*` → `renderpearl.api.pipeline.*`，`blaze3d.PrimitiveTopology` → `renderpearl.api.pipeline`，`blaze3d.opengl.GlStateManager` → `renderpearl.backend.opengl`，`blaze3d.platform.CompareOp` → `renderpearl.api.pipeline`。`RenderTarget` / `vertex.*` / `platform.*` 原地不动。

**`PoseStack.mulPose(Quaternionfc)` → `rotate(Quaternionfc)`**（约 45 处）。对的实现体逐字相同，只是改了名。

**`ItemInHandRenderer` 被删除**（这是规则里说的"平台删除功能"情形，需要你确认）：26.3 拆成玩家持有的 `FirstPersonHandsAndItems`（`tick`/`mainHandHeight` 等字段全在这里）+ `FirstPersonHandsAndItemsRenderer`。原来的 `ItemInHandRendererMixin` 拆成了两个 mixin，`KeepingItemRenderer.cgc$getRenderer()` 改从 `LocalPlayer.firstPersonHandsAndItems()` 取。为保住"永远返回非空"的原约定、不改动 9 个调用点，加了个 `EMPTY` 空实现兜底。

**模板测试的载体变了**：从 `RenderPipeline` 的独立字段挪进了 `DepthStencilState` 的第 5 个分量。因此 `PipelineModifier.NO_DEPTH_WRITE` 必须显式带上 `stencilTest`，`RenderSystem.applyPipelineModifiers` 消失后注入点改挂到 `PipelineModifierStack.apply`（新增了 `RenderSystemAccessor` 取私有修饰器栈）。

## 需要你判断的语义取舍

1. **`CommandSourceStack` 不再接受 `textName`/`displayName`** —— 名字只能由 entity 推导（`NamesProvider`）。4 个 core 事件 + 19 个 neoforge 包装事件按"有 entity 就用 entity 构造器"统一处理。副作用：`ProjectileHitBlockEvent` 的名字会从"方块名"变成"射物名"。已在 `IEvent.createCommandSourceStack` 的 javadoc 里写明。
2. **`entityTranslucentCullItemTarget` 连同 `OutputTarget.ITEM_ENTITY_TARGET` 整体被删除**，映射到 `entityTranslucentCull`（原版自己也是这么改的）。
3. **`_AttachmentModelRender` 去掉了 `target.useStencil` 前置判断** —— 该字段已不存在，只保留 `getDepthTexture() != null`。
4. **`RenderSetup` 变不可变且构造器私有**，`OutputTarget` 被 `OitPipelineSet` 取代、多出 `outlineTextureName`/`forceSolidModelPhase`。我在 `neoforge-compat` 的 AT 里放开了构造器与全部字段（26.2 那批 `outputTarget` 条目已删除，否则会因字段不存在报错）。

## 我没能验证的部分

`core` 和 `neoforge-compat` **没有配置 mixin 注解处理器**（只有未参与构建的 `forge-compat` 有），所以编译通过**不能**证明我新写的 mixin/accessor 目标有效。以下四处只做了人工比对源码，运行期未验证：`FirstPersonHandsAndItemsMixin`、`FirstPersonHandsAndItemsRendererMixin`、`RenderSystemAccessor`、`PipelineModifierStackMixin`。

另外 `forge-compat` 不在 `settings.gradle` 里、未参与构建，我没有动它（它的 AT 里仍有 26.2 的 `RenderSetup` 条目，将来启用需同步）。

按规则我没有提交。要我接着跑 `:neoforge-compat:runClient` 验证 mixin 是否真的挂得上吗？

# 

```
/fork 我现在把CommandSourceStack的兼容方式照搬了BattleRoyale的（见1.20.1分支）：
- 我现在已经在CGC core1.20.1复制了utils，你按照CBR commit "Back-port 26.3 CommandSourceStack diffs to 1.20.1"相同的方式处理CGC里的CommandSourceStack调用
- 注意CBR里出现过不能直接搜"return new CommandSourceStack("找到的，有的是"return this.source != null ? this.source : new CommandSourceStack("
- IDEA提供的MCP应该有全局浏览构造函数的引用情况的，确保只有utils类里有引用（确保封装）

1. 先完成core1.20.1（forge）的部分
2. 然后在CGC 1.20.2分支的neoforge里完成对称的部分（不要写forge），这个时候应该是不能编译通过的（还没合并）
3. 移除CGC 26.3分支里对CommandSourceStack的修改

完成以上三步后，我手动将1.20.1的修改merge到所有分支，并将1.20.2neoforge的修改cherry pick（所以你最后要移除掉26.3里的CommandSourceStack修改）
```

# 

I'll start by studying the BattleRoyale commit and the current state of the CGC worktrees.

Let me see the target shape of `CommandUtils` on the 26.3 and 1.20.2 branches of BattleRoyale, plus what's already in CGC.

Now I understand the design. Let me examine CGC's current state.

All 23 call sites match the `sourceStack` signature exactly. Let me rewrite them.

Now let me cross-check with IDEA MCP and a repo-wide search that nothing else references the constructor.

Now let me compile the 1.20.1 branch.

Step 1 is done (core + forge-compat build clean). Now step 2 — the 1.20.2 worktree.

Step 2: only `neoforge-compat` in the 1.20.2 worktree. Let me verify the call shape there first.

Now let me confirm the expected state — the 1.20.2 neoforge module should fail only on the not-yet-merged `CommandUtils`.

Step 2 is exactly as expected (all 39 errors are the unmerged `CommandUtils`). Now step 3 — reverting the 26.3 branch's CommandSourceStack edits.

All 24 files contain only CommandSourceStack work. Let me revert them.

All three steps are done. Let me run a final verification pass.

All three steps verified. Let me record the pattern the user established.

三步都完成了。

## 1. CGC 1.20.1（core + forge-compat）✅ 编译通过

按 CBR 的做法重写了 23 处调用点：`new CommandSourceStack(` → `CommandUtils.sourceStack(`，并每文件补一行 import。

- core/api/event：4 处（GunFireEvent、ProjectileHitBlockEvent、ProjectileHitEntityEvent、LivingShooterEvent）
- forge-compat/forge/event：19 处

`./gradlew build` → BUILD SUCCESSFUL（`:core:compileJava`、`:forge-compat:compileJava`、两个 jar 都实际执行）。

封装性已确认：仓库内只剩 `CommandUtils.java` 引用该构造器（5 处，其中 1 处在 javadoc 注释里），IDEA MCP 的全局搜索与文件系统 grep 结果一致。

补一句：CGC 1.20.1 里 23 处全是平铺的 `return new CommandSourceStack(`，没出现你说的 `return this.source != null ? this.source : new CommandSourceStack(` 那种形式。不过我是按子串 `new CommandSourceStack(` 替换的，那类写法一样能覆盖。

## 2. CGC 1.20.2 的 neoforge-compat ✅ 按要求只改 neoforge

19 处同样重写，`forge-compat` 与 `core` 未动（`git status` 只有那 19 个 `neoforge/event/Neo*.java`）。

编译结果符合预期：共 39 个错误，**全部**是 `CommandUtils` 找不到符号（19 处 import + 19 处调用），没有任何其它错误 —— 就是"还没合并"的状态。

## 3. CGC 26.3 已移除 CommandSourceStack 的修改 ✅

回退了 24 个文件（5 个 core/api/event + 19 个 neoforge/event）到 HEAD。回退前逐行核对过这些文件的 diff，确认里面只有 CommandSourceStack 相关的改动（删除 textName/displayName、我之前加的两条注释和 IEvent 的 javadoc）。

**保留了** `neoforgeclient/event/NeoInputKeyEvent.java` 的 `getScanCode()` → `getKeycode()` —— 那是 KeyEvent 第二分量改名的适配，不属于 CommandSourceStack。

⚠️ 26.3 分支现在编译不过了（那 24 处又回到了 26.3 不接受的 9 参形式），等你 merge 1.20.1 + cherry-pick 1.20.2neoforge 后就恢复。

## 后续 merge 时要注意的一点

合并后 26.3 的 `CommandUtils` 需要换成 26.3 形态：`commandLevel` 用 `PermissionSet`，`sourceStack` 改为转发到 `_SourceStack.entity/.name` 并把 `// 26.3移除` 的那几个参数注释掉。CBR 的 `26.3neoforge` 分支上就是现成的样子（`_SourceStack.entity/name` 里 `""` 和 `Component.empty()` 两行被注释掉）。

需要我现在就按 CBR 26.3 的形态把这份文件准备好吗？还是你自己在 merge 时处理。

# 

```
非渲染部分已经back-port同步好了，现在处理渲染问题。

经26.3测试：
- 装上红点瞄准镜（enableSight=true），准心会在ocular以外的范围可见，也就是待机非开镜状态下就能看到红点；4倍镜（enableScope=true）ocular全黑（模型本身的颜色）
- 刚才修了26.2的一个模板残留问题，以上提到的问题在这个修复前就有，修复后仍然存在
- 由于26.3默认是没有debug.log的，我把latest.log放在了D:\Temp-File\latest.log

需要排查26.3渲染问题
```

# 

Non-render parts synced — now on the render problems. Let me start with the log and the current state of the render path.

Found a smoking gun. Let me scan all mixin application results.

Only one mixin fails to apply: `GameRendererMixin`. Let me read it and the 26.3 target.

`GameRenderer.render()` takes no arguments in 26.3 — that stale `@Shadow` kills the whole mixin. Let me fix that, then investigate the stencil path.

Let me read the ocular/scope render path in detail.

Let me check whether the pipelines CGC stencils actually carry a `DepthStencilState` in 26.3.

Let me check whether `RenderType.create` caches by name in 26.3.

Let me check the submit-ordering mixin, which memory flags as critical for the stencil sequence.

This looks like the key change. Let me diff the 26.2 vs 26.3 submit-phase ordering.

Let me check whether I can reach the tested jar to confirm what build was actually run.

Let me check the stencil operation mapping — `INVERT`/`REPLACE` are what the ocular masks depend on.

Let me trace which RenderType reaches the ocular mask draws.

I've exhausted static analysis — I need runtime evidence. First let me fix the one confirmed defect, then wire in targeted diagnostics.

Let me add temporary diagnostics to pin down where the stencil is lost.

Let me improve the diagnostic to dedupe by distinct state, so one run gives the full picture.

Build passes. Let me confirm the jar output path so the test procedure is unambiguous.

已定位到一个确定的缺陷，另外加了一组临时诊断，需要你跑一次。

## 确定的缺陷（已修）

`latest.log` 里只有一处 mixin 失败：

```
Mixin apply for mod customgun failed customgunclient.mixins.json:renderer.GameRendererMixin
@Shadow method render(Lnet/minecraft/client/DeltaTracker;Z)V ... was not located
```

26.3 的 `GameRenderer.render()` 已经没有参数了（`public void render()`），那个 `@Shadow` 又没被任何注入点使用 —— 文件自己的注释就写着「不需要的方法就不要去shadow」。它一失效，整个 `GameRendererMixin` 报废，`bobView`/`bobHurt` 的镜头摇晃抵消全部丢失。已删除该 shadow。

**其余 mixin（含我新写的 4 个）在这个日志里都没有报错**，说明它们都挂上了。

## 静态排查已排除的项

- `StencilTest` / `StencilPerFaceTest` / `StencilOperation` 与 26.2 逐字相同（只有 `CompareOp` 换了包）
- `DEBUG_TRIANGLE_FAN`、`ENTITY_CUTOUT_CULL` 等实际被模板化的管线**都带 `DepthStencilState`**，所以我在 `applyStencilToPipeline` 里那条「没有深度状态就丢弃模板」的兜底不是这次的元凶
- `RenderType.create` / `prepare()` 没有按名字做缓存，烘焙出来的模板不会被合并掉
- 26.3 的 `RenderPipeline#toBuilder()` 已正确复制颜色目标；我这边也仍然显式写回

症状（红点准心在 ocular 外可见、4x 目镜全黑）**方向是一致的：模板测试根本没作用到那些 draw 上**。

## 需要你跑一次

临时诊断已经加好（`RenderDiag`，按内容去重打印，不会刷屏）。请重新构建并复现一次：

```bash
./gradlew build
```

用 `neoforge-compat/build/libs/customgun-0.0.15-neoforge-26.3.jar` 进游戏，装上红点瞄准镜和 4 倍镜，开镜 + 待机各看一眼，然后把 latest.log 放回 `D:\Temp-File\latest.log`。

日志里我只关心这些行：

```bash
grep CGC-RENDER-DIAG latest.log
```

这组日志能一次区分出四种情况：

- **完全没有 `[mixin]`** → `PipelineModifierStack.apply` 的 hook 没生效（注入点问题）
- **有 `[mixin]` 但没有 `[inj-ok]`，全是 `[inj-skip]`** → hook 活着，但烘焙时 `CURRENT_STENCIL` 是空的（设置/读取时序问题）
- **有 `[inj-noDS]`** → 确实遇到了没有 `DepthStencilState` 的管线（我之前判断错）
- **有 `[inj-ok]` 且 `[bake]` 显示 `stencil=...`** → 模板成功烘焙进 RenderType，问题在下游（draw 期的管线解析或提交顺序），我再往那层查

诊断代码是临时的，定位完我会连同 `RenderDiag` 一起删掉。

# 

```
还是不行，日志在D:\Temp-File\latest.log
```

# 

Diagnostics show the stencil **is** being injected and baked correctly. Let me see the rest.

Found it. Let me confirm where `NO_DEPTH_TEST` is used.

Let me read the render dispatch to see which path each sight type takes.

The confirmed bug: `NO_DEPTH_TEST` strips the `DepthStencilState`, which in 26.3 also removes the stencil. Let me check the impact of keeping a depth state.

Root cause #1 confirmed. `NO_DEPTH_TEST` removes the `DepthStencilState` entirely, and in 26.3 the stencil lives inside it — so the red-dot crosshair loses its mask. Let me fix it.

Now let me add diagnostics for the 4x scope path so one more run answers both.

诊断跑通了，找到了红点那个症状的根因。

## 诊断结论

日志证明**模板注入链路本身是好的**——从 `[set]` → `[mixin]` → `[inj-ok]` → `[bake ... stencil=StencilTest[...]]` 全部正确，而且 draw 期用 `.../cgc_stencil` 回查时模板也原样带过去了。

真正的断点在最后几行：

```
[set] ref=0 frontCompare=EQUAL frontPass=REPLACE
[mixin] pipeline=.../entity_cutout_cull/transform/customgun/no_depth_test
[inj-noDS] 管线没有 DepthStencilState，模板被丢弃
```

`renderSight`（红点）画准心前会 `_disableDepthTest()` → 推入 `NO_DEPTH_TEST`，而 `NO_DEPTH_TEST` 的实现是 `withDepthStencilState(Optional.empty())`。

**26.2 时模板是 `RenderPipeline` 上的独立字段，整块移除深度状态不影响它；26.3 把模板塞进了 `DepthStencilState`，所以移除深度状态 = 连模板一起抹掉。** 准心于是完全失去模板遮罩，在 ocular 之外整片可见 —— 正是你看到的现象。

## 修复

在 `applyStencilToPipeline` 里：管线没有 `DepthStencilState` 时，补一个等价的 `DepthStencilState(ALWAYS_PASS, writeDepth=false, 0, 0, stencil)`（就是原来「关闭深度测试」的语义）只为把模板挂上去。

**没有去改 `NO_DEPTH_TEST` 本身**，原因值得记一下：GUI 的 `AttachmentCategorySlot` / `RefitTurnPageButton` 等也用它，而 26.3 的 `GUI_SNIPPET` 不带深度状态、GUI 渲染通道因此没有深度附件，`FrontendRenderPass` 遇到 `wantsDepthTexture()` 为真的管线会直接抛 `IllegalStateException`。只在「确实有模板要挂」时补状态，就绕开了这个雷。

顺带修掉了上一轮提到的 `GameRendererMixin` 的 `@Shadow render(DeltaTracker, boolean)`（26.3 已变成无参 `render()`），那个 mixin 之前整个是失效的。

## 4x 那个症状还没定位

日志里 `renderScope` 的遮罩序列（写目镜 1 → 镜身 0 → INVERT 圆 1 → 黑色遮罩 1 → 准心 254）是完整正确的，所以 4x 全黑应该是另一个原因。我加了两条针对性日志：

- `[dispatch]` —— 确认走的是 `renderBoth` / `renderScope` / `renderSight` 哪条
- `[circle]` —— 打印 INVERT 圆的 `radiusModifier` 与 `finalRad`

我怀疑是圆的半径。代码里 `rad *= aimingProgress`，如果 `cgc$getRenderAimingProgress()` 返回 0，圆就退化成一点 → 模板没被 INVERT 翻转 → 黑色遮罩（`GL_EQUAL i+1`）盖满整个目镜 → 全黑。`[circle]` 一行就能证实或排除。

麻烦重新构建后再测一次，**红点和 4x 各开镜、待机各看一遍**：

```bash
./gradlew build
```

jar 在 `neoforge-compat/build/libs/customgun-0.0.15-neoforge-26.3.jar`，跑完把日志放回 `D:\Temp-File\latest.log`。这次我主要看：

```bash
grep -E "CGC-RENDER-DIAG\]\[(dispatch|circle|inj-noDS|inj-ok)\]" latest.log
```

# 

```
红点修复了，4倍镜还没有，日志还是在Temp-File
```

# 

红点确认修好了。让我看 4x 的诊断数据。

半径在正常增长（0→25.8），所以不是圆退化的问题。让我看 4x 那一段的完整遮罩序列。

半径正常，遮罩序列也完整。让我检查 26.3 的管线缓存是按键还是按身份。

红点的修法生效了，说明遮罩序列本身没问题。4x 用的是 INVERT 圆 + 黑色遮罩，圆的位置依赖 model-view 矩阵。让我核对一下这个 API。

让我把诊断升级为「定位圆心 + 确认目镜写入 + 实际绘制顺序」，一次跑完就能定论。

Now let me add executed-draw-order tracing so the next run is decisive.

构建通过。4x 的症状锁定在两个可能：INVERT 圆画错位置，或者它的绘制被排到了黑色遮罩之后。这次的日志能一次分开。

## 目前掌握的事实

从上一份日志能确认 4x 的遮罩序列**本身是完整且正确的**：

```
[dispatch] scope=true sight=false entries=1     ← 走 renderScope
[inj-ok] entity_cutout_cull ref=0      →  [bake] ALWAYS_PASS  ref=0    目镜外环
[set] EQUAL/KEEP ref=0                 →  [bake] EQUAL/KEEP   ref=0    镜身（stencil==0）
[circle] finalRad 0 → 25.8（随开镜进度增长）
[inj-ok] debug_triangle_fan ref=1      →  [bake] EQUAL/INVERT ref=1    INVERT 圆
[inj-ok] entity_cutout_cull ref=1                                       黑色遮罩
[inj-ok] entity_cutout_cull ref=254    →  [bake] EQUAL/KEEP   ref=254  准心
```

半径排除了（`radiusModifier=1.0`，开镜时半径正常涨到 25.8）。所以「目镜全黑」只可能是 **INVERT 圆没在镜片位置挖出孔**：

- 圆画在别处 → 镜片内 stencil 仍是 1 → 黑色遮罩（`GL_EQUAL 1`）盖满整个镜片 → 全黑
- 或者圆被排到黑色遮罩之后 → 遮罩先盖、圆后翻 → 同样全黑

## 这次加的探针

- `[ocularStencil]` —— `renderOcularStencil` 实际写入了几个目镜条目（`enableScopeArg=false` 对 4x 是否匹配）
- `[circleCenter]` —— 圆的原始中心 `ocularCenter` 与换算后的 `centerX/centerY`
- `[drawOrder]` —— **实际执行的绘制顺序**（按 `PreparedRenderType.drawFromBuffer` 记录带模板的管线，帧末汇总成一条），新增两个临时 mixin

麻烦再构建跑一次，装 4x 镜 **开镜看几秒**（让半径涨起来），然后把日志放回 `D:\Temp-File\latest.log`：

```bash
./gradlew build
```

jar 同路径。我这次看这几行：

```bash
grep -E "CGC-RENDER-DIAG\]\[(ocularStencil|circleCenter|drawOrder)\]" latest.log
```

如果 `[drawOrder]` 里 254/EQUAL/INVERT 出现在 1/EQUAL/KEEP（黑色遮罩）**之前**，就是绘制顺序被打乱；如果它本来就排在前面，那就是圆心算错了 —— 两种情况下一步的修法完全不同，所以这一跑能把方向定死。

# 

```
还是没修好，日志还是在Temp-File下
```

# 

`[drawOrder]` 给出了答案——INVERT 圆被执行顺序排到了最后：

```
0/ALWAYS_PASS/KEEP  1/GREATER_THAN/REPLACE  0/EQUAL/KEEP  1/EQUAL/KEEP  254/EQUAL/KEEP  ...  1/EQUAL/INVERT
                                          ↑ 黑色遮罩      ↑ 准心            ↑ 圆在最后
```

让我查 26.3 的渲染阶段分配。

**Found it.** 26.3 splits Custom geometry across two phases executed at different times. Let me confirm the routing rule.

**确认根因**。让我看一下 `SubmitNodeCollection` 的分流条件。

**根因确认**：`submitCustomGeometry` 按 `RenderType.hasBlending()` 分流到两个**执行时间不同**的阶段：

```java
else if (renderType.hasBlending()) this.translucentCustomGeometry.submit(submit);
else this.solid.submit(submit);
```

`debugTriangleFan` 的管线带 `BlendFunction.TRANSLUCENT` → INVERT 圆被排到 **translucent 阶段**，晚于 solid 阶段的黑色遮罩执行，孔自然挖不出来。让我确认 `_colorMask` 的使用范围再动手。

`_colorMask` 只用在模板遮罩这两处 —— 正好覆盖所有模板**写入**。让 `NO_COLOR_WRITE` 同时去掉混合即可让它们留在 solid 阶段。

找到根因了。这次的 `[drawOrder]` 日志是决定性的。

## 根因

26.3 的 `SubmitNodeCollection#submitCustomGeometry` 会按 `RenderType#hasBlending()` 把自定义几何**分派到两个执行时机不同的阶段**：

```java
if (renderType.isOutline())            this.outline.submit(submit);
else if (renderType.hasBlending())     this.translucentCustomGeometry.submit(submit);
else                                   this.solid.submit(submit);
```

而 `FeatureRenderDispatcher.renderAllFeatures` 是 `executeSolid` → `executeTranslucent` 顺序执行。日志里实际执行的顺序正好印证了这一点：

```
0/ALWAYS_PASS/KEEP   1/GREATER_THAN/REPLACE   0/EQUAL/KEEP   1/EQUAL/KEEP   254/EQUAL/KEEP  ...  1/EQUAL/INVERT
                                                ↑ 黑色遮罩     ↑ 准心                    ↑ INVERT 圆被推到最后的 translucent 阶段
```

INVERT 圆用的是 `RenderTypes.debugTriangleFan()`，它的管线来自 `DEBUG_FILLED_SNIPPET`，带 `ColorTargetState(BlendFunction.TRANSLUCENT)` → `hasBlending() == true` → 被排进 translucent 阶段。而外环/镜身/黑色遮罩/准心用的 `entity_cutout_cull` 用的是 `ColorTargetState.DEFAULT`（无混合）→ 留在 solid 阶段。

于是执行顺序变成：先写目镜 → 镜身 → **黑色遮罩（此时圆还没翻转，遮罩盖满整个镜片）** → 准心（`GL_EQUAL 254`，此时还没有 254，全被丢弃）→ 最后才轮到 INVERT 圆。孔永远挖不出来。

`_clearStencilBuffer`、模板注入、`NO_DEPTH_TEST`（红点那个）都没问题 —— 纯粹是**阶段错位导致模板写入被排到模板测试之后**。

## 修法

`NO_COLOR_WRITE` 本来就只用在模板遮罩这两处（`renderOcularStencil` 的写入 + `renderOcularAndDivision` 的 INVERT 圆），而这两处都关了颜色写出。既然一个颜色都不写，混合就没有意义 —— 让 `NO_COLOR_WRITE` 顺手把混合函数也去掉，`hasBlending()` 就变 false，这些模板写入会回到 solid 阶段，与模板测试重新同阶段、顺序恢复。

这个改动是收敛的：`_colorMask` 全项目只在这两处用，其它带混合的模板绘制（比如 `entity_translucent` 的第一人称手臂）都是**模板测试**而非写入，跨阶段不影响，不会被误伤。

麻烦再构建验证一次，4x 镜开镜看几秒：

```bash
./gradlew build
```

日志放回 `D:\Temp-File\latest.log`，我重点看这行是不是变成 `... 0/EQUAL/KEEP 1/EQUAL/INVERT 1/EQUAL/KEEP 254/EQUAL/KEEP`（INVERT 圆排到遮罩**前面**）：

```bash
grep "CGC-RENDER-DIAG\]\[drawOrder\]" latest.log
```

确认修好后我会把 `RenderDiag` 和那三个临时探针（`PreparedRenderTypeMixin`、`RenderTypeFeatureRendererMixin`、`_AttachmentModelRender` 里的日志）全部删掉。

# 

```
修好了，日志在Temp-File，报告一下这个问题
```

# 

修好了。让我先确认日志里的顺序，然后清理临时探针。

执行顺序已恢复正确：`... 1/EQUAL/INVERT 1/EQUAL/KEEP 254/EQUAL/KEEP` —— INVERT 圆排在黑色遮罩和准心之前了。现在清理所有临时探针。

构建通过，诊断代码已全部清除。让我确认最终改动。

让我把这几个坑记到项目记忆里（沿用已有 26.x 参考笔记的模式）。

修好了，诊断代码已全部撤掉。

## 问题报告：26.3 目镜模板遮罩失效

### 症状

| 配件 | 症状 |
|---|---|
| 红点（`enableSight`） | 准心在 ocular 之外整片可见，待机时也能看到红点 |
| 4 倍镜（`enableScope`） | 目镜全黑（模型本身颜色），看不到孔 |

两者是**两个独立的根因**，恰好都出在 26.3 对「模板测试」的重构上。

---

### 根因一：模板测试被挪进 `DepthStencilState`（红点）

26.2 里模板测试是 `RenderPipeline` 上的独立字段，所以「整块移除深度状态」的修饰器不会连带抹掉模板。26.3 把模板塞进了 `DepthStencilState` 的第 5 个分量 —— 两者绑定在一起了。

`renderSight` 画准心前会 `_disableDepthTest()` → 推入 `NO_DEPTH_TEST`，其实现是 `withDepthStencilState(Optional.empty())`。模板随之消失，准心没了遮罩。

**修法**：在 `applyStencilToPipeline` 里，若管线已经没有深度状态，补一个等价的 `DepthStencilState(ALWAYS_PASS, writeDepth=false, …)` 只为把模板挂上去。

**没有直接改 `NO_DEPTH_TEST`**，这里有个坑值得记：GUI（`AttachmentCategorySlot` / `RefitTurnPageButton` 等）也用它，而 26.3 的 `GUI_SNIPPET` 不带深度状态、GUI 渲染通道因此**没有深度附件**，`FrontendRenderPass` 遇到 `wantsDepthTexture()` 为真的管线会直接抛 `IllegalStateException`。只在「确实有模板要挂」时才补状态，绕开了这个雷。同理 `NO_DEPTH_WRITE` 改写深度状态时必须显式带上 `stencilTest`。

---

### 根因二：自定义几何被拆进两个执行时机不同的渲染阶段（4 倍镜）

`SubmitNodeCollection#submitCustomGeometry` 按 `RenderType#hasBlending()` 分派：

```java
if (renderType.isOutline())        this.outline.submit(submit);
else if (renderType.hasBlending()) this.translucentCustomGeometry.submit(submit);
else                               this.solid.submit(submit);
```

而 `FeatureRenderDispatcher.renderAllFeatures` 是 `executeSolid` → `executeTranslucent` 顺序执行 —— **两个阶段的执行时间不同**。

INVERT 圆用的 `RenderTypes.debugTriangleFan()` 来自 `DEBUG_FILLED_SNIPPET`，带 `BlendFunction.TRANSLUCENT` → 被排进 translucent 阶段；而外环/镜身/黑色遮罩/准心用的 `entity_cutout_cull` 用 `ColorTargetState.DEFAULT`（无混合）→ 留在 solid 阶段。实际执行顺序被日志抓了个正着：

```
修复前: 0/ALWAYS_PASS  1/GREATER_THAN/REPLACE  0/EQUAL  1/EQUAL/KEEP  254/EQUAL/KEEP  ...  1/EQUAL/INVERT
                                                         ↑黑色遮罩    ↑准心              ↑圆被推到最后的 translucent 阶段
修复后: 0/ALWAYS_PASS  1/GREATER_THAN/REPLACE  0/EQUAL  1/EQUAL/INVERT  1/EQUAL/KEEP  254/EQUAL/KEEP
                                                        ↑圆回到遮罩之前
```

于是：先写目镜 → 镜身 → **黑色遮罩盖满整个镜片（此时圆还没翻转）** → 准心（`GL_EQUAL 254`，此时根本没有 254，全被丢弃）→ 最后才轮到 INVERT 圆。孔永远挖不出来。

**修法**：`NO_COLOR_WRITE` 只用在模板遮罩这两处，而这两处都关了颜色写出 —— 既然一个颜色都不写，混合就没有意义。让它顺手把颜色目标的混合函数也去掉，`hasBlending()` 变 false，这些模板**写入**回到 solid 阶段，与模板测试重新同阶段、顺序恢复。

改动是收敛的：全项目 `_colorMask` 只在这两处出现；其它带混合的模板绘制（如 `entity_translucent` 的第一人称手臂）都是模板**测试**而非写入，跨阶段不影响，不会被误伤。

顺带记一个排查经验：**`forceSolidModelPhase()` 对 customGeometry 无效** —— 那个分支只看 `hasBlending()`，不看 force 标志，所以不能靠它解决问题。

---

### 途中一并修掉的

`GameRendererMixin` 因为 `@Shadow render(DeltaTracker, boolean)` 在 26.3 已变成无参 `render()`，导致**整个 mixin 失效**，`bobView`/`bobHurt` 的镜头摇晃抵消全部丢失。这个 shadow 没被任何注入点使用，直接删除。

### 排查手段

加了一组临时探针（`RenderDiag` + `PreparedRenderType` / `RenderTypeFeatureRenderer` 两个 mixin），记录**实际执行**的绘制顺序而不是提交顺序 —— 提交侧看是完全正确的，只有执行侧才暴露阶段错位。这套探针在确认修复后已全部删除，最终改动只保留修复本身。

### 实际改动的文件

- `PipelineModifier.java` —— `NO_COLOR_WRITE` 去混合；`NO_DEPTH_WRITE` 保留 `stencilTest`
- `NeoStencilOperator.java` —— 无深度状态时补 `ALWAYS_PASS` 深度状态挂模板；注入点迁到 `PipelineModifierStack.apply`
- `PipelineModifierStackMixin.java`（新增，替代 `RenderSystemMixin`）
- `GameRendererMixin.java` —— 删除失效 shadow

`./gradlew build` 通过。这次是实机验证过的（之前构建通过但不等于 mixin 挂得上 —— `core`/`neoforge-compat` 没有配置 mixin 注解处理器，这一点我记到项目笔记里了）。
